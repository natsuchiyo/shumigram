'use client';

import { chakra } from "@chakra-ui/react";
import { FontAwesomeIcon } from "@fortawesome/react-fontawesome";



export const AwesomeIcon = chakra(FontAwesomeIcon, {
    baseStyle: {
        marginRight: '2',
    },
});
